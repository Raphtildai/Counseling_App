import 'package:flutter/material.dart';

class RescheduleSession extends StatelessWidget {
  final String docID;
const RescheduleSession({ Key? key, required this.docID }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}